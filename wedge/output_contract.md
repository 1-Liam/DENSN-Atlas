# Output Contract

The wedge produces:

- a missing invariant or hidden state candidate
- a reusable abstraction or remapped abstraction when transfer is valid
- verifier-backed evidence for acceptance
- explicit reasons when transfer is blocked

Every accepted output must pay rent on held-out tasks. Every blocked output must include a verifier-visible reason whenever feasible.
