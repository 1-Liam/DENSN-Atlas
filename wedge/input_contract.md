# Input Contract

The wedge consumes artifact bundles with these components:

- natural-language specification
- formal rules or formal specification
- execution traces
- failing tests
- logs
- counterexamples
- executable validator or verifier manifest when available

Each input item must be provenance-linkable so verifier feedback and counterexamples can flow back into the graph.
